// Class with instance variables
class Car {
    // Instance variables
    String model;
    int year;
    String color;

    // Constructor to initialize instance variables
    Car(String model, int year, String color) {
        this.model = model;
        this.year = year;
        this.color = color;
    }

    // Method to display car details
    void displayDetails() {
        System.out.println("Car Model: " + model);
        System.out.println("Year: " + year);
        System.out.println("Color: " + color);
    }
}

public class Main {
    public static void main(String[] args) {
        // Creating objects (each object has its own instance variables)
        Car car1 = new Car("Toyota Corolla", 2020, "Red");
        Car car2 = new Car("Honda Civic", 2019, "Blue");

        // Displaying details of car1
        car1.displayDetails();
        System.out.println();

        // Displaying details of car2
        car2.displayDetails();
    }
}
